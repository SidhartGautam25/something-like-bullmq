/*


in this stage we will add clranup feature

why cleanup ??
      -> Redis has limited memory.
      -> Old jobs (completed, failed, dead) pile up forever unless cleaned.
      -> We want to auto-delete them after X seconds/days.




*/
